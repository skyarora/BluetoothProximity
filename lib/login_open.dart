import 'package:flutter/material.dart';
import 'login_screen.dart';

class LoginOpen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MyLoginPage(),
    );
  }
}